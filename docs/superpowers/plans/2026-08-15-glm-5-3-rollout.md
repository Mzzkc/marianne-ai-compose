# GLM 5.3 Rollout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make one-million-context GLM 5.3 the active Claude Code and Marianne GLM default, run it at maximum reasoning, and publish bounded-task and authorized defensive-security routing doctrine.

**Architecture:** Preserve the existing Claude Code and OpenCode executor boundaries and change profile data, fleet routing data, and their contract tests. Keep volatile model names in the live instrument catalog/composing layer; keep conducting doctrine provider-neutral. Machine configuration and private profiles are validated separately from repository source.

**Tech Stack:** YAML instrument profiles, Claude Code settings JSON, OpenCode CLI, Pydantic v2 profile parsing, Pytest, Marianne compiler presets, Markdown skills.

## Global Constraints

- Use `glm-5.3[1m]` for the Claude Code gateway and `zai-coding-plan/glm-5.3` for OpenCode.
- Every GLM 5.3 declaration has a 1,000,000-token context window and 131,072-token output limit.
- Request `max` reasoning through `--effort max` for Claude Code and `--variant max` for OpenCode.
- Preserve GLM 5 Turbo and GLM 4.x specialist routes.
- Security guidance applies only to owned or explicitly authorized defensive targets and must not weaken instrument guardrails.
- Preserve the unrelated modified `scores/prep/thinking-lab-input/prompt.md` on `main`.

---

### Task 1: Built-in Claude Code Profile Contract

**Files:**
- Modify: `tests/test_builtin_instrument_profiles.py`
- Modify: `src/marianne/instruments/builtins/claude-code.yaml`

**Interfaces:**
- Consumes: `InstrumentProfile.model_validate(dict)`
- Produces: built-in `claude-code` model `glm-5.3[1m]` and fixed `--effort max` invocation

- [ ] Add `test_claude_code_has_glm_5_3_max_reasoning` asserting the exact model name, context/output limits, absence of `glm-5.2[1m]`, and the command tail `['--effort', 'max']`.
- [ ] Run `PYTHONPATH="$PWD/src" pytest tests/test_builtin_instrument_profiles.py::TestProfileDetails::test_claude_code_has_glm_5_3_max_reasoning -q` and observe an assertion failure against GLM 5.2.
- [ ] Replace the built-in GLM entry and add the fixed effort flags.
- [ ] Re-run the targeted profile test and the complete `tests/test_builtin_instrument_profiles.py` file.

### Task 2: Generic Fleet Routing Contract

**Files:**
- Modify: `tests/test_generic_fleet_preset_contract.py`
- Modify: `compiler/tests/test_builtin_presets.py`
- Modify: `compiler/tests/test_compose_instruments.py`
- Modify: `compiler/src/marianne_compiler/assets/generic-fleet.yaml`

**Interfaces:**
- Consumes: compiler built-in preset loader and instrument resolver
- Produces: generated alias `claude-code--glm-5.3-1m` with model `glm-5.3[1m]`

- [ ] Change current expectations from GLM 5.2 to GLM 5.3 in the three contract files.
- [ ] Run the affected targeted tests and observe failures against the unchanged preset.
- [ ] Replace every current `glm-5.2[1m]` route in the generic fleet asset, retaining GLM 5 Turbo.
- [ ] Re-run all three affected test files.

### Task 3: Instrument Catalog and Durable Doctrine

**Files:**
- Create: `plugins/tests/test_glm_5_3_guidance.py`
- Modify: `plugins/marianne/docs/ref/instrument-catalog.yaml`
- Modify: `plugins/marianne/docs/ref/instrument-catalog.md`
- Modify: `plugins/marianne/docs/ref/CHANGELOG-instrument-catalog.md`
- Modify: `plugins/marianne/skills/composing/SKILL.md`
- Modify: `plugins/marianne/skills/conducting/references/marianne-operations.md`

**Interfaces:**
- Consumes: current catalog YAML and the conducting/composing routing boundary
- Produces: current GLM 5.3 selection guidance plus provider-neutral specialist delegation doctrine

- [ ] Add tests asserting the catalog's GLM 5.3 limits, bounded high/max calibration, defensive-security suitability, all current use-case repoints, composing guidance, and provider-neutral conducting safeguards.
- [ ] Run `PYTHONPATH="$PWD/src" pytest plugins/tests/test_glm_5_3_guidance.py -q` and observe failures because the guidance is absent.
- [ ] Add the GLM 5.3 catalog entry, preserve GLM 5.2 as historical, repoint current chains, update the generated-view warning/changelog, and add the two skill rules.
- [ ] Re-run the new test and `plugins/tests/test_conducting_skill_release.py`.

### Task 4: Machine and Private Profile Rollout

**Files:**
- Modify: `/home/emzi/.claude/settings.json`
- Modify: `/home/emzi/.marianne/instruments/claude-code.yaml`
- Modify: `/home/emzi/.marianne/instruments/opencode.yaml`
- Modify: `/home/emzi/.marianne/instruments/flowspec-q5c2-sandbox.yaml`
- Modify: `/home/emzi/.marianne/instruments/opencode-gemma.yaml` (current-route comment only)

**Interfaces:**
- Consumes: Claude Code model aliases and OpenCode's live Z.AI model registry
- Produces: active global/private GLM 5.3 defaults at maximum reasoning

- [ ] Patch only model, capacity, reasoning, and current-route comment fields; do not touch credentials.
- [ ] Parse settings with `jq` and all profiles with `InstrumentProfile.model_validate` bound to candidate source.
- [ ] Run `mzt instruments check` for `claude-code`, `opencode`, and `flowspec-q5c2-sandbox`.
- [ ] Confirm `opencode models zai-coding-plan --verbose` reports the expected live limits.

### Task 5: Live Probes and Release Verification

**Files:**
- Modify: `/home/emzi/Projects/legion/legion.md` (append only)

**Interfaces:**
- Consumes: active machine/private config and candidate repository source
- Produces: verified live routing and durable handoff memory

- [ ] Run a bounded Claude Code probe with `glm-5.3[1m] --effort max` and a bounded OpenCode probe with `zai-coding-plan/glm-5.3 --variant max`.
- [ ] Run import provenance, targeted suites, compiler suites, Ruff, Mypy, and the full applicable Pytest suite one at a time.
- [ ] Run a final current-authority census for GLM 5.2 and classify any remaining hits as historical or stale.
- [ ] Append the verified rollout and its evidence boundaries to Legion memory.
- [ ] Commit repository changes without staging the user's unrelated main-worktree modification.
