# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# ═══════════════════════════════════════════════════════════════════════════════
# Marianne Repository Organization
# See .marianne/spec/conventions.yaml (Repository Structure) for full rules
# ═══════════════════════════════════════════════════════════════════════════════

# Workspaces — all job workspaces live under workspaces/ (or legacy dot-prefixed).
# Contents are ignored; the tracked README.md scaffold keeps the directory
# present on fresh clones (path validation V002 expects it).
.worktrees/
workspaces/*
!workspaces/README.md
*-workspace/
*-workspace-*/
*workspace-*/
.daemon-workspace-*/
test-workspace-*/

# Internal scores — development/QA scores, not for public consumption
scores-internal/

# Session handoff documents
handoffs/

# Logs
*.log
logs/

# Test artifacts
tests/temp/

# Coverage artifacts
.coverage
coverage*.json
dashboard_coverage.json
healing_coverage.json
htmlcov/

# Investigation/debug artifacts
investigation-results/
debug-*.yaml
temp-scores/

# Internal planning docs (live outside the repo now; guard stays)
docs/plans/
# Legacy top-level scripts/ (test-coupled helpers moved to tests/scripts/)
/scripts/

# Marianne state files (generated during job execution)
.marianne-state.db
.marianne-outcomes.json
.marianne-pause-*

# Top-level score YAML (generated by evolution jobs, not source)
marianne-*.yaml
marianne-opus-evolution-v*.yaml

# Project-specific context (personal, not for public repo)
.marianne/
memory-bank/
STATUS.md
CLAUDE.local.md
.claude
.playwright*

# Exceptions: tracked directories that might match patterns above
!examples/
!scores/

# State files (keep structure, ignore contents)
*.json.tmp

# MkDocs generated site
site/

# Hypothesis property-based testing cache
.hypothesis/

# Flowspec (cache and generated output — config.yaml IS tracked)
.flowspec/cache/
.flowspec/*
flowspec*

# OS
.DS_Store
Thumbs.db
.aider*
